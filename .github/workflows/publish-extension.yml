name: Publish VS Code Extension

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 0.1.1)'
        required: false

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build extension
        run: npm run package
      
      - name: Package VSIX
        run: npx vsce package -o archpulse-vscode.vsix
      
      - name: Check OVSX Token
        run: |
          if [ -z "${{ secrets.OVSX_TOKEN }}" ]; then
            echo "⚠️ OVSX_TOKEN is not set. Skipping Open VSX publish."
            echo "To publish to Open VSX, add OVSX_TOKEN to repository secrets."
          else
            echo "✅ OVSX_TOKEN is configured"
          fi
      
      - name: Publish to Open VSX
        if: ${{ secrets.OVSX_TOKEN != '' }}
        run: npx ovsx publish archpulse-vscode.vsix -p ${{ secrets.OVSX_TOKEN }}
        continue-on-error: true
      
      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: archpulse-vscode
          path: vscode-extension/archpulse-vscode.vsix
      
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: vscode-extension/archpulse-vscode.vsix
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
